\name{NEWS}
\title{NEWS in package 'AFLPsim'}
\encoding{UTF-8}
\section{Changes in version 0.3-3}{
      \itemize{  
      \item{Fixed bug ( \code{gscan} to run in BxA)}
      \item{Fixed bug ( \code{hybridsim} in fragment selection for BxB and F2 individuals)}
      \item{Fixed bug ( \code{plot.hybridsim} plots BxA and BxB hybrids without F1 individuals)}
      \item{Small changes in \code{hybridsim} and \code{hybridize} Documentation}
      }}
\section{Changes in version 0.2-2}{
      \itemize{  
      \item{Fixed bug (Wrong behavior of neutral selection simulation in       \code{hybridsim} and \code{hybridize}).}
      \item{Fixed bug ( \code{hybridsim} and \code{hybridize} transform S to S^10)}
      \item{Fixed bug ( \code{plot.hybridsim} plots BxA and BxB hybrids)}
      }}
\section{Changes in version 0.1-4}{
      \itemize{  
      \item{Updated \code{bayescan} to run in Mac systems}
      \item{Fixed Rd usage lines to be less than 90 characters long}
      \item{Changed \code{hybridindex} example to run for a few seconds}
      }}
      

